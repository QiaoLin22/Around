
version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.9
    working_directory: /go/src/github.com/laioffer/Around
    steps:
      - checkout
      - setup_remote_docker
      - run: |
          docker build -t qlin22/around .
          docker login -u="$qlin22" -p="$Linqiao970613!"
          docker push qlin22/around

